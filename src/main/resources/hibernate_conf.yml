spring:
  datasource:
    url: jdbc:h2:mem:test_db
    username: postgres
    password: postgres
    driverClassName: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
